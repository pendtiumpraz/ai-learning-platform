// Dashboard test page removed to fix build errors
// Authentication testing can be done directly on the main dashboard

export default function DashboardTestRemoved() {
  return (
    <div className="min-h-screen bg-gray-50 p-8">
      <div className="max-w-4xl mx-auto">
        <h1 className="text-3xl font-bold mb-4">Dashboard Test Page Removed</h1>
        <p>This test page has been removed to resolve build errors. Authentication testing can be done on the main dashboard at /dashboard</p>
      </div>
    </div>
  )
}